version: "3"

includes:
  utils:
    internal: true
    taskfile: "../tools/yscope-dev-utils/exports/taskfiles/utils/utils.yaml"

vars:
  G_DEV_DOCS_OUTPUT_DIR: "{{.G_BUILD_DIR}}/dev-docs-html"
  G_HOMEPAGE_OUTPUT_DIR: "{{.ROOT_DIR}}/docs/homepage/out"
  G_NODE_DEPS_DIR: "{{.G_BUILD_DIR}}/node-deps"
  G_PROJECTS_DOCS_DIR: "{{.G_BUILD_DIR}}/project-docs"
  G_PUBLISHABLE_OUTPUT_DIR: "{{.G_BUILD_DIR}}/html"
  G_VENV_CHECKSUM_FILE: "{{.G_BUILD_DIR}}/docs#venv.md5"
  G_VENV_DIR: "{{.G_BUILD_DIR}}/venv"

tasks:
  build:
    deps:
      - "build-dev-docs"
      - "build-homepage"
    cmds:
      - "rm -rf '{{.G_PUBLISHABLE_OUTPUT_DIR}}'"
      - "mkdir -p '{{.G_PUBLISHABLE_OUTPUT_DIR}}'"
      - >-
        rsync -a --exclude='/index.html'
        "{{.G_DEV_DOCS_OUTPUT_DIR}}/"
        "{{.G_PUBLISHABLE_OUTPUT_DIR}}/"
      - "rsync -a '{{.G_HOMEPAGE_OUTPUT_DIR}}/' '{{.G_PUBLISHABLE_OUTPUT_DIR}}/'"
      - task: "symlink-project-docs"

  clean:
    deps:
      - "clean-dev-docs"
      - "clean-homepage"
    cmd: "rm -rf '{{.G_PUBLISHABLE_OUTPUT_DIR}}'"

  download-projects:
    deps: ["venv"]
    cmds:
      - |-
        . "{{.G_VENV_DIR}}/bin/activate"
        python3 scripts/download-projects.py --build-dir "{{.G_PROJECTS_DOCS_DIR}}"

  release-tar:
    deps: ["build"]
    cmd: >-
      tar czf "{{.G_BUILD_DIR}}/yscope-docs.tar.gz"
      -h
      -C "$(dirname "{{.G_PUBLISHABLE_OUTPUT_DIR}}")"
      "$(basename "{{.G_PUBLISHABLE_OUTPUT_DIR}}")"

  serve:
    deps:
      - "build"
      - "http-server"
    cmd: |-
      npm --prefix '{{.G_NODE_DEPS_DIR}}' exec http-server '{{.G_PUBLISHABLE_OUTPUT_DIR}}' -c-1

  build-dev-docs:
    internal: true
    vars:
      CHECKSUM_FILE: "{{.G_BUILD_DIR}}/{{.TASK | replace \":\" \"#\"}}.md5"
      OUTPUT_DIR: "{{.G_DEV_DOCS_OUTPUT_DIR}}"
    sources:
      - "{{.G_VENV_CHECKSUM_FILE}}"
      - "{{.ROOT_DIR}}/docs/dev-docs/**/*"
      - "{{.TASKFILE}}"
    generates: ["{{.CHECKSUM_FILE}}"]
    run: "once"
    deps:
      - task: "utils:checksum:validate"
        vars:
          CHECKSUM_FILE: "{{.CHECKSUM_FILE}}"
          INCLUDE_PATTERNS: ["{{.OUTPUT_DIR}}"]
      - "venv"
    cmds:
      - |-
        . "{{.G_VENV_DIR}}/bin/activate"
        sphinx-build -a -E -c docs/dev-docs -b html docs/dev-docs "{{.OUTPUT_DIR}}"

      # This command must be last
      - task: "utils:checksum:compute"
        vars:
          CHECKSUM_FILE: "{{.CHECKSUM_FILE}}"
          INCLUDE_PATTERNS: ["{{.OUTPUT_DIR}}"]

  build-homepage:
    internal: true
    vars:
      CHECKSUM_FILE: "{{.G_BUILD_DIR}}/{{.TASK | replace \":\" \"#\"}}.md5"
      OUTPUT_DIR: "{{.G_HOMEPAGE_OUTPUT_DIR}}"
    sources:
      - "{{.ROOT_DIR}}/docs/homepage/**/*"
      - "{{.TASKFILE}}"
      - exclude: "docs/homepage/.next/**/*"
      - exclude: "docs/homepage/node_modules/**/*"
      - exclude: "docs/homepage/next-env.d.ts"
      - exclude: "docs/homepage/out/**/*"
      - exclude: "docs/homepage/public/robots.txt"
      - exclude: "docs/homepage/public/sitemap*.xml"
    generates: ["{{.CHECKSUM_FILE}}"]
    run: "once"
    deps:
      - task: "utils:checksum:validate"
        vars:
          CHECKSUM_FILE: "{{.CHECKSUM_FILE}}"
          INCLUDE_PATTERNS: ["{{.OUTPUT_DIR}}"]
    cmds:
      - |-
        cd docs/homepage || exit 1
        npm install --no-audit --no-fund
        npm run build

      # This command must be last
      - task: "utils:checksum:compute"
        vars:
          CHECKSUM_FILE: "{{.CHECKSUM_FILE}}"
          INCLUDE_PATTERNS: ["{{.OUTPUT_DIR}}"]

  clean-dev-docs:
    internal: true
    run: "once"
    cmd: "rm -rf '{{.G_DEV_DOCS_OUTPUT_DIR}}'"

  clean-homepage:
    internal: true
    run: "once"
    cmds:
      - "rm -rf '{{.G_HOMEPAGE_OUTPUT_DIR}}'"
      - "rm -rf docs/homepage/.next"
      - "rm -rf docs/homepage/node_modules"
      - "rm -f docs/homepage/next-env.d.ts"
      - "rm -rf docs/homepage/out"
      - "rm -f docs/homepage/public/robots.txt"
      - "rm -f docs/homepage/public/sitemap-0.xml"
      - "rm -f docs/homepage/public/sitemap.xml"

  http-server:
    internal: true
    vars:
      CHECKSUM_FILE: "{{.G_BUILD_DIR}}/{{.TASK | replace \":\" \"#\"}}.md5"
      OUTPUT_DIR: "{{.G_NODE_DEPS_DIR}}"
    sources:
      - "{{.ROOT_TASKFILE}}"
      - "{{.TASKFILE}}"
    generates: ["{{.CHECKSUM_FILE}}"]
    run: "once"
    deps:
      - ":init"
      - task: "utils:checksum:validate"
        vars:
          CHECKSUM_FILE: "{{.CHECKSUM_FILE}}"
          INCLUDE_PATTERNS: ["{{.OUTPUT_DIR}}"]
    cmds:
      - "rm -rf '{{.OUTPUT_DIR}}'"
      - "npm --prefix '{{.OUTPUT_DIR}}' install http-server"

      # This command must be last
      - task: "utils:checksum:compute"
        vars:
          CHECKSUM_FILE: "{{.CHECKSUM_FILE}}"
          INCLUDE_PATTERNS: ["{{.OUTPUT_DIR}}"]

  # For each project in `G_PROJECT_DOCS_DIR`, and each version of a project, symlinks their built
  # docs (if they exist) into the publishable output directory.
  symlink-project-docs:
    internal: true
    run: "once"
    cmds:
      - |-
        cd "{{.G_PROJECTS_DOCS_DIR}}" || exit 0
        for project_dir in */; do
          project_name="${project_dir%/}"

          for version_dir in "${project_dir}"/*/; do
            version_name=$(basename "${version_dir}")

            built_docs_dir="${version_dir}/build/docs/html"
            if [ -d "${built_docs_dir}" ]; then
              link_target="{{.G_PUBLISHABLE_OUTPUT_DIR}}/${project_name}/${version_name}"
              mkdir -p "$(dirname "${link_target}")"
              ln -sf "$(readlink -f "${built_docs_dir}")" "${link_target}"
            else
              echo "Warning: No built docs found for ${project_name}@${version_name}"
            fi
          done
        done

  venv:
    internal: true
    vars:
      CHECKSUM_FILE: "{{.G_VENV_CHECKSUM_FILE}}"
      OUTPUT_DIR: "{{.G_VENV_DIR}}"
      REQUIREMENTS_FILE: "{{.ROOT_DIR}}/requirements.txt"
    sources:
      - "{{.REQUIREMENTS_FILE}}"
      - "{{.TASKFILE}}"
    generates: ["{{.CHECKSUM_FILE}}"]
    run: "once"
    deps:
      - ":init"
      - task: "utils:checksum:validate"
        vars:
          CHECKSUM_FILE: "{{.CHECKSUM_FILE}}"
          INCLUDE_PATTERNS: ["{{.OUTPUT_DIR}}"]
    cmds:
      - task: "utils:misc:create-venv"
        vars:
          LABEL: "docs"
          OUTPUT_DIR: "{{.OUTPUT_DIR}}"
          REQUIREMENTS_FILE: "{{.REQUIREMENTS_FILE}}"

      # This command must be last
      - task: "utils:checksum:compute"
        vars:
          CHECKSUM_FILE: "{{.CHECKSUM_FILE}}"
          INCLUDE_PATTERNS: ["{{.OUTPUT_DIR}}"]
